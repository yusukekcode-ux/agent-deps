# agent-deps

Curated list of public APM agent-skill dependencies. This repo has no skill
content of its own — it only declares dependencies in `apm.yml` so that
consumers can depend on the whole set via a single `git:` reference.
